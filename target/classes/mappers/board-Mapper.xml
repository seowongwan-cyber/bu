<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swg.blog.BoardMapper">

	<insert id="create">
		insert into blog_board (title, userid, content)
		values(#{title},#{userid}, #{content})
	</insert>

	<select id="read" resultType="com.swg.blog.vo.BoardVO">
		select
		bno, title, userid, content,
		regdate, viewcnt
		from
		blog_board 
		where bno = #{bno}
	</select>
	
	<update id="update">
		update blog_board  set title =#{title}, content
		=#{content}
		where bno = #{bno}
	</update>

	<delete id="delete">
		delete from blog_board  where bno = #{bno}
	</delete>

	<select id="listAll" resultType="com.swg.blog.vo.BoardVO">
 		<![CDATA[
 			select 
   				bno, title, userid, content, regdate, viewcnt 
 			from 
   				blog_board
 			where bno > 0 
 				order by bno desc, regdate desc
		 ]]>
	</select>
</mapper>

